{
  "name": "muallim-firestore-rules-tests",
  "version": "1.0.0",
  "description": "Firestore security rules test suite for muallim-123",
  "scripts": {
    "test": "jest --testTimeout=30000 --forceExit",
    "emulator:start": "firebase emulators:start --only firestore"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}